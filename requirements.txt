fastapi>=0.100.0  # The core framework
uvicorn[standard]>=0.23.0 # ASGI server + performance extras (uvloop, httptools)
pydantic>=2.0.0 # Data validation and settings management (FastAPI relies heavily on it)